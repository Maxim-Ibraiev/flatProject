const routes = {
  home: '/',
  about: '/about',
  portfolio: '/portfolio',
  prices: '/prices',
  resume: 'https://maxim-ibraiev.github.io/resume/',
  getTour: (id: string | number) => `/${id}`,
}

export default routes
