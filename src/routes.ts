const routes = {
  home: '/',
  about: '/about',
  portfolio: '/portfolio',
  prices: '/prices',
  resume: 'https://maxim-ibraiev.github.io/resume/',
  getFlat: (id: string | number) => `/${id}`,
}

export default routes
